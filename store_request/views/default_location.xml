<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_stock_location_form_inherit_default" model="ir.ui.view">
        <field name="name">stock.location.form.inherit.default</field>
        <field name="model">stock.location</field>
        <field name="inherit_id" ref="stock.view_location_form"/>
        <field name="arch" type="xml">
            <!-- Add the field inside the first group -->
            <xpath expr="//field[@name='return_location']" position="after">
                <field name="is_default_location" string="Default Location"/>
            </xpath>
        </field>
    </record>
</odoo>